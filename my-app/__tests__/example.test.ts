describe('Example Test', () => {
  it('works', () => {
    expect(1).toBe(1);
  });

  it('adds numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });

  it('handles strings', () => {
    expect('hello' + ' ' + 'world').toBe('hello world');
  });
});
